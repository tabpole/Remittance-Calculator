import 'package:shadcn_flutter/shadcn_flutter.dart';

class SetupScreen extends StatelessWidget {
  const SetupScreen({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Center(
      child: const Text("Settings"), // Placeholder for Receiving tab
    );
  }
}
